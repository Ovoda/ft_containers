#include <map>
#include <vector>
#include <iostream>


int main () {
    std::vector<int> _m(12, 12);
    std::cout << _m.capacity() << std::endl;
	_m.resize(72);
    std::cout << _m.capacity() << std::endl;
    
	_m.resize(100);
    std::cout << _m.capacity() << std::endl;
    return (0);
}